- name: Create sample FirewallRules
  azure_rm_firewallrules:
    resource_group_name: ansible-demo-rg
    server_name: zims-server
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
