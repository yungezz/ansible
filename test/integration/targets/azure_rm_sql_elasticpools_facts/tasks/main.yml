- name: Create instance of ElasticPools
  azure_rm_sql_elasticpools_facts:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
