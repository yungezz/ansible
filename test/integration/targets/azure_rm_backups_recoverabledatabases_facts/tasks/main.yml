- name: Create instance of RecoverableDatabases
  azure_rm_backups_recoverabledatabases_facts:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
