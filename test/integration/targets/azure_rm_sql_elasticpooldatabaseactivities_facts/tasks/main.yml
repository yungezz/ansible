- name: Create instance of ElasticPoolDatabaseActivities
  azure_rm_sql_elasticpooldatabaseactivities_facts:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
