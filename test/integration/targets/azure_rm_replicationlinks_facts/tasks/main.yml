- name: Create instance of ReplicationLinks
  azure_rm_replicationlinks_facts:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
