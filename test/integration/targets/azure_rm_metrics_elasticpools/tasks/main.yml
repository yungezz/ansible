- name: Create sample Metrics
  azure_rm_metrics_elasticpools:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
