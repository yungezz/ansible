- name: Create instance of VirtualNetworkRules
  azure_rm_virtualnetworkrules_facts:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
