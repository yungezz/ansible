- name: Create DataMasking
  azure_rm_datamasking_datamaskingrules_facts:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
