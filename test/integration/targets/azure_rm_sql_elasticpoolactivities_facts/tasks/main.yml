- name: Create instance of ElasticPoolActivities
  azure_rm_sql_elasticpoolactivities_facts:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
