- name: Create instance of ServerAzureADAdministrators
  azure_rm_serverazureadadministrators_facts:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
