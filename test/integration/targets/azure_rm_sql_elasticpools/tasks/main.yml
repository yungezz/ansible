- name: Create sample Sql
  azure_rm_sql_elasticpools:
    resource_group_name: ansible-demo-rg
    server_name: zims-server
    elastic_pool_name: zims-elastic-pool
    location: westus
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
