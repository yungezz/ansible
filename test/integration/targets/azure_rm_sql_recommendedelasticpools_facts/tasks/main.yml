- name: Create instance of RecommendedElasticPools
  azure_rm_sql_recommendedelasticpools_facts:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
