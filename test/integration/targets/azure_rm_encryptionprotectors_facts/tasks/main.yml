- name: Create instance of EncryptionProtectors
  azure_rm_encryptionprotectors_facts:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
