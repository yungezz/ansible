- name: Create instance of ServerConnectionPolicies
  azure_rm_serverconnectionpolicies:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
