- name: Create instance of BackupLongTermRetentionVaults
  azure_rm_backuplongtermretentionvaults:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
