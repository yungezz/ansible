- name: Create instance of DataMaskingPolicies
  azure_rm_datamasking_datamaskingpolicies:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
