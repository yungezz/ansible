- name: Create instance of BackupLongTermRetentionPolicies
  azure_rm_backuplongtermretentionpolicies:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
