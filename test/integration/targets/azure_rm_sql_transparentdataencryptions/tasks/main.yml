- name: Create instance of TransparentDataEncryptions
  azure_rm_sql_transparentdataencryptions:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
